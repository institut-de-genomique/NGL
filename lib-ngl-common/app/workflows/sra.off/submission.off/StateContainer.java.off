package workflows.sra.submission;

import models.laboratory.common.instance.ITracingAccess;
import models.laboratory.common.instance.State;

/**
 * State container (object contenant un état).
 * 
 * @author sgas
 *
 */
public interface StateContainer extends ITracingAccess  {
	
	/**
	 * Get object state.
	 * @return object state
	 */
    State getState(); // inutile de mettre public abstract car on est dans interface
    
    /**
     * Set object state.
     * @param state new state
     */
    void setState(State state);
        
//	TraceInformation getTraceInformation();
	
}
